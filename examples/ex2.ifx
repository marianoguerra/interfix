if A is less than 3:
    do something with A.

if A is less than 3:
    do something with A;

if A is bigger than 4:
    log A
    do something with A.


if A is less than 3:

    log A
    do something with A;

if A is bigger than 4:

    log A
    do something with A.

A is (1 + 2)
is A (1 * 2)?

if A matches 42:
  print 42;
match to :bar:
  print :bar;
.

fn divide A by B:
    :ok (A + B);
fn divide A by 0:
    :error :divide_by_zero.
